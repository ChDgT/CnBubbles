console.log('hello')

function refreshPendings (newList) {
  const list  = document.querySelector("#pending-list");
  list.innerHTML = newList
}

refreshPendings('<%= j render "bookings/pending_list", booking: @booking, comic: @booking.comic %>');
